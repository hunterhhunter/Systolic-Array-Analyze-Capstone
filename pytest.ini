[pytest]
testpaths = tests
pythonpath = .
addopts = -ra
markers =
    e2e: requires IREE/SCALE-Sim runtime and generated artifacts
    slow: long-running simulation or model conversion tests
    model: requires optional torch/transformers/onnx dependencies
