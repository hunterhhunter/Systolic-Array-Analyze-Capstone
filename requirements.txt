# Runtime dependencies for npu-capston Phase 3-6 pipeline.
# Refresh with: make freeze   (requirements.lock 생성 후 비교)
#
# Pinning policy:
#   - 이미 .venv에 설치된 버전은 exact-pin (재현성)
#   - Phase 4+에서 처음 쓰는 패키지는 lower-bound 허용 (joblib, pyarrow, matplotlib)
#   - iree-compiler/iree-runtime은 make env 후 실 버전으로 update 예정

# Core numerics + data
numpy==2.4.4
pandas==3.0.2
pyarrow>=15.0.0

# Sweep orchestration (Phase 4)
joblib>=1.3.0

# Plotting (Phase 6)
matplotlib>=3.8.0

# IREE Python binding (mlir2scalesim.py 1순위 파서)
# NOTE: PyPI dev snapshot (2024-11-04) — 로컬 IREE 빌드(2026-03-21, VERSIONS.md 참조)와
#       약 1년 5개월 drift. mlir2scalesim.py 구현 시 Module.parse()/walk() 호환성 재검증.
iree-compiler==20241104.1068
iree-runtime==20241104.1068

# SCALE-Sim은 리포 내장 fork (SCALE-Sim/). `make env`에서 -e 로 설치.
